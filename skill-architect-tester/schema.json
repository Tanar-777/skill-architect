{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "name": "skill-architect-tester",
  "description": "Execute and test code in a skill directory — runs pytest and optionally scripts, writes code-test-report.md",
  "parameters": {
    "type": "object",
    "properties": {
      "skill_path": {
        "type": "string",
        "description": "Direct path to the skill directory"
      },
      "skill_name": {
        "type": "string",
        "description": "Skill name, resolved under ~/.claude/skills/"
      },
      "run_scripts": {
        "type": "boolean",
        "description": "Whether to also execute scripts/*.py files (default: false)",
        "default": false
      },
      "caller": {
        "type": "string",
        "description": "Caller context — suppresses interactive prompts when called programmatically",
        "enum": ["skill-architect", "workflow", "standalone"]
      }
    }
  }
}
